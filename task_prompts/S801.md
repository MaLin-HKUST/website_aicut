# Task: S801 — 退役 draft/current 系列接口

## 你的角色
你是一个专注的软件开发代理。你的任务是完成下面描述的一个具体小任务，
并在完成后进行**严格的三层验证**（代码审查 → 测试 → 端到端）。

## ⚠️ 核心原则：验证优先
> **没有通过端到端验证的代码不得 commit。**
> 实现只占 30% 工作量，验证占 70%。

## 项目基本信息
- 这是一个 FastAPI + Next.js + SQLAlchemy 项目
- 工作目录: /Users/malin13/Documents/trae_projects/website_aicut
- 当前分支: feature/smart-cut-taskcard-refactor-S801
- 主分支: feature/smart-cut-taskcard-refactor
- 大任务: 旧代码清理与文档全面收口 (L08)

## 任务描述
退役 draft/current 系列接口

**现状**
- `apps/api/routes/tasks.py` 中仍然保留：
  - `GET /api/smart-cut/tasks/draft/current`
  - `POST /api/smart-cut/tasks/draft/current/ensure`
  - `POST /api/smart-cut/tasks/draft/current/abandon-all`
- 前端已不再调用，但接口仍在响应

**具体工作**
1. 在接口处理函数中添加废弃标记，返回 `410 Gone`：
   ```python
   @router.get("/draft/current")
   def get_draft_current(...):
       raise HTTPException(
           status_code=410,
           detail="This endpoint is retired. Use POST /api/smart-cut/tasks/start instead."
       )
   ```
2. 删除内部辅助函数 `_find_current_hidden_draft()`
3. 检查并删除 `draft/current` 相关的前端调用代码（确认已没有）
4. 更新 API 文档

**验证标准**
- [ ] 调用 `GET /api/smart-cut/tasks/draft/current` 返回 410
- [ ] 调用 `POST /api/smart-cut/tasks/draft/current/ensure` 返回 410
- [ ] 前端控制台无相关报错
- [ ] 全局搜索 `_find_current_hidden_draft` 无结果

**预计耗时**：30 分钟

---

## 涉及的关键文件（你需要自己读取）
- apps/api/routes/tasks.py (draft/current 接口)

## 执行步骤
1. 读取上述关键文件，理解当前实现
2. 按照任务描述进行**最小化修改**
3. **运行三层验证**（见下方验证章节）
4. **只有全部验证通过后**才执行：
   ```bash
   git add 修改的文件
   git commit -m "[S801] 退役 draft/current 系列接口"
   ```

## 三层验证（必须全部通过才能 commit）

### L1 代码审查验证
修改完成后，确认以下文件**确实被修改了**：
- [ ] `git diff` 显示 `apps/api/routes/tasks.py` 有变更
确认以下文件**没有被意外修改**：
- [ ] `git diff --name-only` 不包含 `apps/web/`
- [ ] `git diff --name-only` 不包含 `apps/models/`

### L2 测试运行验证
运行测试命令：
```bash
cd /Users/malin13/Documents/trae_projects/website_aicut
pytest tests/test_rel0415_api.py -k draft -v
```
- [ ] 所有测试通过（exit code 0）

### L3 端到端验证（⚠️ 最重要的验证层）
运行以下端到端验证命令：

**命令 1：**
```bash
curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:18002/api/smart-cut/tasks/draft/current || echo 'API not running - manual verification required'
```
- [ ] 输出包含 `410`

**命令 2：**
```bash
curl -s -o /dev/null -w '%{http_code}' -X POST http://127.0.0.1:18002/api/smart-cut/tasks/draft/current/ensure || echo 'API not running - manual verification required'
```
- [ ] 输出包含 `410`


### 验证未通过的处置
如果任何一层验证失败：
1. **不要 commit**
2. 修复问题
3. 重新运行失败的验证
4. 只有全部通过后才能 commit


## 完成后
1. 在控制台输出执行摘要（2-3 句话）
2. 确保所有修改已 commit
3. 确保工作目录干净（`git status` 无未提交修改）

## 约束
- 只修改与当前任务直接相关的文件
- 不要修改 task.json、progress.md 等文档文件
- 不要修改其他 task 的代码
- 如果遇到困难，先尝试解决；如果确实无法完成，说明原因并尽可能完成部分工作
- 不要运行 git push
- **没有验证通过不要 commit**

