# 2026-04-19 当前系统状态

本文回答什么问题：

- 2026-04-19 这一刻，A 机器和 Worker 机器实际上跑着什么
- 正式公网入口现在指向哪里
- 当前运行面里哪些是正式保留，哪些只是历史目录但不再承载流量
- 后续 Agent 接手时，应该先从哪里理解当前生产状态

## 一句话结论

到 2026-04-19 为止，系统已经从“旧主站线 + `release_0415_slot` 前端线 + 旧调度线 + A 机过渡 Worker 线并存”收敛成：

- A 机器：
  - 一套正式公网前端入口
  - 一套 legacy 网站 API / SQLite
  - 一套 candidate 调度中心
  - 一套正式 TOS 工具链
- Worker 机器：
  - 一套 Worker Gateway
  - 通过 SSH tunnel 接入 A 机器 candidate 调度中心
- TOS：
  - 正式输入输出交换层
  - 正式备份归档层

## 正式公网入口

- 域名：`xiaomajianji.cn`
- 入口机器：A 机器 `14.103.249.104`
- nginx 当前 upstream：`http://127.0.0.1:3301`
- 当前公开页面验证：
  - `/login` 命中：`小马AI剪辑`、`营销视频剪辑智能体`、`登录`
  - `/welcome` 命中：`小马 AI 剪辑`、`小马AI准备就绪`、`请点击左侧`
  - `/tts` 命中：`把文案直接转成音频`、`当前音色`

## A 机器当前运行面

A 机器：`14.103.249.104`

### 当前保留的正式运行面

1. host web
- 角色：正式公网前端
- 绑定：`127.0.0.1:3301`
- 运行方式：宿主机 `next-server`

2. `website_aicut_api_1`
- 角色：legacy 网站 API / admin / auth
- 绑定：`127.0.0.1:8000`
- 数据库：`/home/malin/website_aicut/data/website_aicut.db`

3. `a-machine-phase5-postgres`
- 角色：candidate 调度 PostgreSQL
- 绑定：`0.0.0.0:55433`

4. `a-machine-phase5-api`
- 角色：candidate 调度 API
- 绑定：`0.0.0.0:18001`

5. `a-machine-phase5-scheduler`
- 角色：candidate Scheduler Runtime

### A 机器当前监听端口

- `443`：nginx HTTPS
- `80`：nginx HTTP
- `22`：SSH
- `127.0.0.1:3301`：正式前端
- `127.0.0.1:8000`：legacy 网站 API
- `0.0.0.0:18001`：candidate 调度 API
- `0.0.0.0:55433`：candidate PostgreSQL

### A 机器上的正式工具资产

- 正式部署根：`/home/malin/website_aicut_prod`
- TOS 工具链：`/home/malin/website_aicut_prod/tools/tos_uploader`
- 预重建备份根：`/home/malin/website_aicut_backups/pre_rebuild_20260418_224329`

### A 机器上仍存在但不再承载正式流量的历史对象

这些对象现在不是正式运行面，但物理目录可能仍然存在：

- `/home/malin/release_0415_slot`
- `/home/malin/website_aicut`
- 旧阶段脚本与历史构建残留

注意：
- 这些目录当前不再承担正式公网流量
- 但它们尚未全部做文件级清尾
- 未来清理时，必须以本目录和回滚锚点为准，不允许凭感觉删

## Worker 机器当前运行面

Worker 机器：`14.103.63.252`

### 当前保留的正式运行面

1. `worker1-phase6-gateway`
- 角色：正式 Worker Gateway
- 镜像：`smart-cut-worker-gateway:0.0.3-dev`
- 健康状态：`healthy`
- Worker ID：`worker1-phase6`

### Worker 机器上的辅助/无关容器

以下容器当前存在，但不属于本轮 `website_aicut` 主链路：

- `capcut-mate`
- `linux`

后续 Agent 不应把它们误认成 `website_aicut` 正式运行面的一部分。

### Worker 与 A 机器的连接方式

当前 Worker 通过本地 SSH tunnel 接回 A 机器 candidate 调度中心：

- `127.0.0.1:65433 -> A:55433`
- `127.0.0.1:61001 -> A:18001`

这说明当前 Worker 侧不是直接访问 A 机器公网 candidate 端口，而是通过 tunnel 进入。

## 当前数据库职责

### SQLite

- 用途：网站 / admin / auth / legacy 业务
- 实际文件：`/home/malin/website_aicut/data/website_aicut.db`
- 当前仍是正式数据源，不能清空

### PostgreSQL

- 用途：candidate 调度中心
- 当前实例：`a-machine-phase5-postgres`
- 绑定：`0.0.0.0:55433`
- 当前已经参与真实 `Smart Cut` 任务链路

## 当前 TOS 角色

- bucket：`autocut-malin`
- endpoint：`tos-cn-shanghai.volces.com`
- region：`cn-shanghai`

当前 TOS 同时承担：

1. `Smart Cut` 输入输出交换
2. 预重建备份归档
3. release bundle 归档
4. 最终产物归档

## 当前已验证的主链路

已验证通过的闭环：

1. 创建业务任务
2. 直接上传输入到真实 TOS
3. analyze
4. preview
5. finalize
6. 最终结果回写业务任务

当前标准验收任务：

- 业务任务：`8dd1797a-7999-48ab-a0c7-76c1b523bd2c`
- analyze 调度任务：`4f92151c-e6e4-4d20-a3b2-9852afa61b65`
- preview 调度任务：`7143f582-4835-4572-a044-1ff1b8a26eff`
- finalize 调度任务：`f485b696-8f75-494f-9e6e-bbfcd4f244ba`
- active edit：`c1f5751a-3b1d-4448-8384-cdf3a43fc09b`
- 最终视频 TOS key：
  - `smart-cut/8dd1797a-7999-48ab-a0c7-76c1b523bd2c/finalize/final_video.mp4`

## 2026-04-20 真实样本链路补证

真实样本专项任务：

- 业务任务：`c10264ad-5f62-4ad9-9731-bffd0e9ce33c`
- analyze 调度任务：`23f074fa-b87c-4d41-9416-8dc928007c04`
- preview 调度任务：`a872b058-cfc7-491c-86be-6b53b3d35844`
- finalize 调度任务：`5dd044b3-f02b-43d4-8026-050ea58c811b`
- active edit：`394bd0e7-c7e5-4626-a7fc-588d0b8bbe82`

这条任务使用的是真实输入：

- `/Volumes/XIAOMA-A-1T/wei_videodb/C2384_reencoded.mp4`
- `/Volumes/XIAOMA-A-1T/wei_videodb/ref.txt`

当前已经确认：

1. `analyze` 输出不再是 mock 文本，而是真实 Script1、真实 ASR、真实 DelayCut、真实 Audio-A。
2. `preview` 已生成真实 `edited_delay_cuts.json`、`pause_cuts_on_original.json`、`audio_b.mp3`。
3. 最终视频对象已落 TOS：
   - `smart-cut/c10264ad-5f62-4ad9-9731-bffd0e9ce33c/finalize/final_video.mp4`
4. 当前业务任务状态已经是：
   - `status = success`
   - `current_stage = complete`

注意：

- 这条真实链路最终是“功能闭环成功”
- 但 finalize 最后一段自动上传和自动回写仍然没有完全自动化
- 本轮是通过“本机复刻 finalize + 本机上传 TOS + 手工修正 PostgreSQL 状态”完成最终收口

## 2026-04-21 Headed Browser 真实回归补证

新的 headed 浏览器真实任务：

- 业务任务：`542a7196-dbee-4e8f-acb4-7f134f2ba438`
- analyze 调度任务：`14c45353-5cd9-4276-a38f-2eac9694519f`
- preview 调度任务：`8cbeabaa-0544-44b8-b8c4-3a47c93b244d`
- finalize 调度任务：`30be1c22-8694-4940-b9b1-c94a45041bc3`
- active edit：`6a7ed486-fcd8-4bb3-8f28-af2a30430ebb`
- 最终视频 TOS key：
  - `smart-cut/542a7196-dbee-4e8f-acb4-7f134f2ba438/finalize/final_video.mp4`

这轮使用的仍是真实输入：

- `/Volumes/XIAOMA-A-1T/wei_videodb/C2384_reencoded.mp4`
- `/Volumes/XIAOMA-A-1T/wei_videodb/ref.txt`

当前已经确认：

1. 正式站 headed 浏览器路径已经完整跑通：
   - 登录
   - `/smart-cut`
   - 上传
   - analyze
   - preview
   - finalize
   - `/tasks` 下载入口观测
2. 旧 `upload-direct` 在真实视频上先返回 `413`，但当前浏览器链路已经通过：
   - `upload-prepare`
   - 浏览器直传 TOS
   - `upload-complete`
   完成上传。
3. Worker `worker1-phase6-gateway` 日志已经出现：
   - analyze 上传 `script.json / asr.json / delay_cuts.json / audio_a.mp3`
   - finalize 自动上传最终视频
   - `Business task marked as success`
4. A 机 candidate PostgreSQL 当前任务行已经是：
   - `status = SUCCESS`
   - `current_stage = COMPLETE`
5. Playwright live UI 观测已经补跑通过：
   - 用例：`apps/web/tests/rel0415-smart-cut-real-task.spec.ts`
   - 结果：`1 passed`

注意：

- 这轮结论已经把“finalize 只能手工收口”从当前 live chain 风险里降级了
- 但这不等于工作台重构契约已经完整上线
- 当前 A 机 candidate PostgreSQL 的 `smart_cut_tasks` 仍只看到旧字段，尚未看到 repo 里这轮重构要求的：
  - `visible_in_task_center`
  - `task_title`

## 当前热补状态

当前线上 candidate 链路包含热补，不是“完全由标准镜像重建出来”的纯净状态。

已知热补文件：

- `apps/services/tos_service.py`
- `apps/api/routes/tasks.py`
- `worker/stages/run_stage_from_manifest.py`
- `worker/services/algorithm_runner.py`
- `apps/scheduler/scheduler_service.py`

这意味着：

- 当前线上链路是可用的
- 但下一轮必须把这些热补收口进标准镜像和标准发布脚本
- 不能长期依赖“容器内手工补文件”

## 当前最重要的风险

1. candidate API 和 Worker 依赖热补
- 当前功能可用
- 但发布真相还没有完全回到“git pull + build + deploy”

2. Worker Gateway 当前不是纯净镜像
- 当前启动依赖运行时 `pip install tos`
- 这必须回收进正式镜像

3. finalize 自动收口已在最新 live chain 成功，但大文件上传耗时仍偏长
- 最新任务 `542a7196-dbee-4e8f-acb4-7f134f2ba438` 已自动上传最终视频并自动回写业务状态
- `final_video_head.txt` 显示最终对象 `Content-Length: 164697370`
- 同时显示 `x-tos-mp-parts-count: 32`
- 这说明功能已经通，但用户等待时长仍需专项优化

4. A 机器历史目录尚未做文件级彻底清尾
- 当前不承载正式流量
- 但未来清理仍需谨慎，以免误伤回滚资产

5. 真实任务 UI 观测验收已经补齐并升级到最新任务
- Playwright 用例：`apps/web/tests/rel0415-smart-cut-real-task.spec.ts`
- 当前已验证任务：
  - `c10264ad-5f62-4ad9-9731-bffd0e9ce33c`
  - `542a7196-dbee-4e8f-acb4-7f134f2ba438`
- 结论：正式站上的 `smart-cut/<task_id>` 和 `/tasks?taskId=<task_id>` 已能看到试听播放器与下载区域

6. live candidate PostgreSQL schema 与工作台重构契约仍未完全对齐
- 当前 `smart_cut_tasks` 现场表结构里仍没有：
  - `visible_in_task_center`
  - `task_title`
- 这说明“真实链路跑通”与“新工作台 schema 已部署到线上”是两件事，后者仍需继续核对部署与迁移

## 对 Agent 的直接指令

- 如果你只想知道“现在生产上到底在跑什么”，先读本文。
- 如果你要继续做发布、热补收口、历史目录清理、镜像标准化，请继续读：
  - [12_2026-04-19_当前Release与证据索引.md](./12_2026-04-19_当前Release与证据索引.md)
  - [08_验收与回滚手册.md](./08_验收与回滚手册.md)
  - [10_重建执行清单.md](./10_重建执行清单.md)
- 如果你下一步要做“真实数据 Smart Cut 联调”，直接转到：
  - [../20_real_data_chain_2026-04-19/README.md](../20_real_data_chain_2026-04-19/README.md)
